The API can simply be run through visual studio. It shouldn't require anything special.
The API is not hosted on the Azure cloud yet, so you must run it before you are able
to login or register using the offline application.